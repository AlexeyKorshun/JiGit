CREATE TABLE IF NOT EXISTS owner (
  id INTEGER AS Int UNIQUE NOT NULL PRIMARY KEY,
  login TEXT NOT NULL
);

selectAll:
SELECT *
FROM owner;

selectById:
SELECT *
FROM owner
WHERE id = ?;

insertOwner:
INSERT OR REPLACE INTO owner (id, login)
VALUES (?, ?);

clear:
DELETE FROM owner;